class Go::Thread {
  use Go::Task;
  
  # Fields
  has exception : ro string;
  
  has error_id : ro int;
  
  # Class Methods
  native static method new : Go::Thread ($task : Go::Task);
  
  # Instance Methods
  native method join : void ();
  
  native method detach : void ();
  
  native method joinable : int ();
  
  native method DESTROY : void ();
  
}
