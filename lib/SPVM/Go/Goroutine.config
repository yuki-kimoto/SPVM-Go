# Copyright (c) 2023 Yuki Kimoto
# MIT License

my $config = SPVM::Builder::Config->new_c99;

$config->use_resource('Resource::Coro');

$config->add_define(@{$config->get_resource('Resource::Coro')->config->defines});

if ($^O eq 'MSWin32') {
  $config->add_lib('ole32');
}

$config;
