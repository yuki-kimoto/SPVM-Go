# Copyright (c) 2023 Yuki Kimoto
# MIT License

class Go::Schedule::Task {
  version_from Go;
  allow Go::Schedule;
  
  use Go::Schedule;
  use Format;
  
  has schedule : Go::Schedule;
  
  private static method new : Go::Schedule::Task () {
    
    my $self = new Go::Schedule::Task;
    
    return $self;
  }
  
  private method : void () {
    
    my $schedule = $self->{schedule};
    
    my $coroutines = $schedule->{coroutines};
    
    unless (@$coroutines > 0) {
      die "[Unexpected Error]\$schedule->{coroutines} must has at-least one element.";
    }
    
    my $coroutine = $coroutines->shift;
    
    my $deadline = $coroutine->{deadline};
    
    if ($coroutine->{finished}) {
      if (Go->ENV_DEBUG) {
        diag Fn->sprintf("[Go Debug]Finish goroutine. \$task=%p.", $coroutine->{task});
      }
      
      $schedule->{current_coroutine} = undef;
    }
    else {
      $coroutines->push($coroutine);
      
      if ($coroutine->{disable}) {
        if (Go->ENV_DEBUG) {
         diag Fn->sprintf("[Go Debug]Skip disabled goroutine. \$task=%p.", $coroutine->{task});
        }
      }
      else {
        if (Go->ENV_DEBUG) {
          diag Fn->sprintf("[Go Debug]Restart goroutine. \$task=%p.", $coroutine->{task});
        }
        
        my $schedule_task_coroutine = $schedule->{schedule_task_coroutine};
        
        $coroutine->{return_back} = $schedule_task_coroutine;
        
        $schedule->{current_coroutine} = $coroutine;
        
        Go::Coroutine->transfer($schedule_task_coroutine, $coroutine);
      }
    }
  }
  
}
